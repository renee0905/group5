---
---
[
  {% for name in site.men %}
    {

      "name"    : "{{ name.name }}",
      "url"      : "{{ site.baseurl }}{{ name.url }}",
      "Olympics"     : "{{ name.Olympics}}"

    }{% unless forloop.last %},{% endunless %}
  {% endfor %}

{% for name in site.women %}
    {

      "name"    : "{{ name.name }}",
      "url"      : "{{ site.baseurl }}{{ name.url }}",
      "Olympics"     : "{{ name.Olympics}}"

    }{% unless forloop.last %},{% endunless %}
  {% endfor %}

{% for name in site.dance %}
    {

      "name"    : "{{ name.name }}",
      "url"      : "{{ site.baseurl }}{{ name.url }}",
      "Olympics"     : "{{ name.Olympics}}"

    }{% unless forloop.last %},{% endunless %}
  {% endfor %}

{% for name in site.pair %}
    {

      "name"    : "{{ name.name }}",
      "url"      : "{{ site.baseurl }}{{ name.url }}",
      "Olympics"     : "{{ name.Olympics}}"

    }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
